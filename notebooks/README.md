# Notebooks

`sensory_grid_comparison_with_temperature.ipynb` is the primary notebook used for
the 27-configuration direct LLM alignment scoring analysis (scoring date
recorded in the manuscript).

Canonical summary tables live under `../analysis/grid/`. Prefer those CSVs for
auditing published numbers. Re-running the notebook against a live API is optional
and requires your own credentials.
